# Upstream baseline and attribution

Source: https://github.com/zydou/arti (a mirror of the Tor Project's Arti).

Pinned commit: `abdbd50b85bed4bddd8afcb32cbf3db2cb794a43`

Inspected on 2026-09-19. The source checkout was used as a reference, not included as a build dependency. Building and testing this repository requires no network access or upstream checkout.

The Go implementation adapts the wire formats and protocol logic in these files at the pinned commit:

- `doc/dev/Architecture.md`
- `crates/tor-cell/src/chancell.rs`
- `crates/tor-cell/src/chancell/codec.rs`
- `crates/tor-cell/src/chancell/msg.rs`
- `crates/tor-cell/src/relaycell/msg.rs`
- `crates/tor-proto/src/crypto/handshake/ntor.rs`
- `crates/tor-proto/src/crypto/cell/tor1.rs`
- `crates/tor-proto/src/channel/handshake.rs`
- `crates/tor-proto/src/client/channel/handshake.rs`
- `crates/tor-proto/src/channel/circmap.rs`
- `crates/tor-cert/src/lib.rs`
- `crates/tor-cert/src/rsa.rs`

Fixtures:

- `ntor/ntor_test.go` transcribes the fixed client ephemeral key, relay public key and identity, request, reply, and 72-byte key vector from the upstream `ntor.rs` test named `testvec`. The production API only generates fresh ephemeral keys.
- `relaycrypto/testdata/tor_cell_crypt.json` is a mechanical conversion of `crates/tor-proto/testdata/cell_crypt.rs`. The upstream fixture was generated by Tor's relay encryption code. It contains expected 509-byte ciphertexts for cells 0, 1, 2, 10, 20, 30, 40, and 50.
- The relay encryption test copies the three 92-byte input key strings and SHAKE256 seed from the upstream `tor1.rs` vector test. Expected ciphertext is checked in, never regenerated from this implementation.
- `torcert/testdata/arti_certs.json` transcribes CERT_T2, CERT_T4, CERT_T5, CERT_T7, PEER_CERT_DIGEST, PEER_ED, PEER_RSA, and the validation timestamp from `crates/tor-proto/src/channel/handshake.rs`. Upstream records these from a Chutney network. Tests validate them at `2020-09-26T18:01:20Z`, not the current time, because the recorded certificates have expired.
- `channel/fixture_test.go` independently generates fresh certificates and acts as a local TLS protocol peer. It is not a C Tor or Arti process and does not implement circuits.
- `scripts/check_local_tor.py` performs an additional live handshake against a temporary C Tor process. The recorded run used Tor 0.4.9.12 from the [official source release](https://dist.torproject.org/tor-0.4.9.12.tar.gz), SHA-256 `c0d307c9dcdaee4848a8ca53e9d6c4ec92823e4f30be12790b0fbddfc6515f5b`, checked against the official `.sha256sum`. That source/build is a temporary testing dependency, not vendored or linked into the Go implementation.

Upstream offers these components under MIT or Apache-2.0. This port uses the MIT option, and includes the upstream MIT notice in LICENSE. No code from `equix` or `hashx` is included.

Protocol references checked during stage 2:

- [Channel negotiation and authentication](https://spec.torproject.org/tor-spec/negotiating-channels.html)
- [Binary certificate formats](https://spec.torproject.org/cert-spec.html)
- [Channel cell format](https://spec.torproject.org/tor-spec/cell-packet-format.html)
- [Circuit ID selection](https://spec.torproject.org/tor-spec/create-created-cells.html)

Deliberate boundaries and compatibility choices:

- Link protocols 4 and 5 only; VERSIONS negotiation uses the older two-byte circuit-ID framing.
- Repeated VERSIONS cells use negotiated four-byte IDs and are ignored by live channels, per the specification. The codec now supports those frames too.
- CERTS and AUTH_CHALLENGE ignore trailing bytes, as required by the specification. Duplicate certificate types are rejected. Unknown critical Ed25519 certificate extensions fail validation.
- Modern channel authentication requires both identity pins and certificate types 2, 4, 5, and 7. No RSA-only authentication fallback is implemented. The legacy type-5 key-type-1 encoding workaround is retained.
- RSA identity certificates are checked for self-signature, 1024-bit key size, exponent 65537, and validity in addition to the RSA-to-Ed25519 cross-certificate. This is stricter than the pinned Arti code's identity-key extraction and follows the current specification's RSA identity checks.
- Ordinary TLS CA/hostname verification is intentionally not used. The exact TLS leaf DER digest is authenticated by the pinned Tor chain before a channel is returned. TLS resumption is disabled. TLS traffic fingerprint parity is not implemented.
- Circuit IDs are random with the initiator's high bit set. Used IDs are retained until channel close, with a 65,536-allocation cap to bound memory.
- Original relay format and type-2 ntor only. No ntor-v3, CGO, or onion-service cryptography.
- No server-side ntor API. Relay behavior used in tests is only a local simulation.
- Parsers reject incomplete structures and cap allocations to their wire-format bounds. Handshake completion requires exactly 64 reply bytes after removing cell padding.
- Invalid incoming relay authentication permanently invalidates the client cipher state.
- Full 20-byte digest tags are used for authenticated directory-download SENDMEs. General-purpose stream windows, outgoing bulk traffic, and congestion control remain future work.

## Stage 3 directory sources and fixtures

The directory implementation references `crates/tor-netdoc/src/doc/authcert.rs`,
`crates/tor-netdoc/src/doc/netstatus.rs`, `crates/tor-netdoc/src/doc/microdesc.rs`,
and the directory specification at the same pinned upstream commit.

These public fixtures were copied byte-for-byte from `crates/tor-netdoc/testdata2/`:

| Local file | Upstream file |
| --- | --- |
| `directory/testdata/authorities.txt` | `cached-certs` |
| `directory/testdata/consensus.txt` | `cached-microdesc-consensus` |
| `directory/testdata/microdescriptors.txt` | `cached-microdescs.new` |

Upstream identifies them as downloaded and massaged from
[Arti CI job 1468137](https://gitlab.torproject.org/tpo/core/arti/-/jobs/1468137/artifacts/download).
The fixture consensus has four valid authority signatures and seven relays.
Tests verify at `2000-01-01T00:02:30Z`; production uses the current clock.
Only these three public documents were copied. No upstream authority or relay
private keys were imported. Signature mutation/rollback tests generate their
own ephemeral RSA authority keys independently.

Directory references:

- [Authority key certificates](https://spec.torproject.org/dir-spec/creating-key-certificates.html)
- [Consensus formats](https://spec.torproject.org/dir-spec/consensus-formats.html)
- [Microdescriptors](https://spec.torproject.org/dir-spec/computing-microdescriptors.html)
- [HTTP directory URLs](https://spec.torproject.org/dir-spec/general-use-http-urls.html)
- [Path constraints](https://spec.torproject.org/path-spec/path-selection-constraints.html)
- [Relay flow control](https://spec.torproject.org/tor-spec/flow-control.html)

The parser requires canonical LF-terminated wire documents without Tor disk
annotations. It checks original signed bytes, including the space after the
first `directory-signature` keyword, rather than reconstructing text. Unknown
required client protocol versions fail closed. Unknown router/microdescriptor
fields also fail closed for this initial implementation. RSA authority keys
are bounded to 1024–8192 bits with exponent 65537. Missing bandwidth weights
can be parsed but cannot be used for selection; the legacy fallback weighting
algorithm is not implemented. README.md records the remaining bootstrap and
privacy limitations.

`scripts/check_local_directory.py` additionally creates a live localhost
network with one newly generated authority and two additional non-exit relays.
The Go CLI downloads and verifies its current certificate, consensus, and
three microdescriptors and writes the cache. `tor-gencert` and C Tor are only
optional test executables. `scripts/directory_probe.go` is excluded from
normal builds and tests a single BEGIN_DIR fetch plus wrong-ntor rejection.

## Directory/guard lifecycle (Veil 0.4)

The lifecycle references the current [directory client operation rules](https://spec.torproject.org/dir-spec/client-operation.html), [guard algorithm](https://spec.torproject.org/guard-spec/algorithm.html), and [guard parameters/randomization](https://spec.torproject.org/guard-spec/appendices.html), checked on 2026-09-20. Additional implementation references at the pinned Arti commit are `crates/tor-guardmgr/src/{lib,sample,guard}.rs` and `crates/tor-netdir/src/params.rs`.

The default guard context uses a persistent sample and confirmation order, derived primary guards, an outcome-reporting attempt API, and non-primary usability checks. Guard-specific directory failures are tracked separately from connection failures. Retries follow the specification's decorrelated jitter, including its recommended 30-second primary / 10-minute non-primary floors and 6-hour / 36-hour caps. This differs from the pinned Arti implementation's 150-second non-primary floor. The sampled-set threshold follows the current specification's count formulation rather than Arti's bandwidth-fraction formulation. Consensus counts are clamped to 1–1,024 for resource bounds; other supported parameter ranges follow the referenced definitions.

Refresh timing follows the client interval after freshness ends and before validity expires. `Session` reuses a channel across sequential requests, with fresh circuits, bounded draining to END, retired-circuit filtering, and channel teardown on failure/cancellation. The manager only retries transient failures; it does not automatically recover from rejected documents or state corruption. Already verified microdescriptor bytes are reused only when their hashes appear in the new signed consensus; network responses must match the particular request's digest set.

This covers the default unrestricted context, not bridges, user-specified reachability/entry filters, path-bias accounting, or general application circuit management. A recently expired cache is usable only to locate existing directory guards for 24 hours; data access still requires a live snapshot. See README.md for operational boundaries.

## Public bootstrap additions (0.8)

`directory/fast_test.go` transcribes the Tor-generated CREATE_FAST vector from
`crates/tor-proto/src/crypto/handshake/fast.rs` in the upstream checkout. It checks
key confirmation and the first 92 derived key bytes used by Veil; production
handshakes always generate fresh random client input. The public authority and
fallback data comes from official Tor Project Arti sources, separately recorded
with source hashes and retrieval date in [directory/data/README.md](directory/data/README.md).
